import { connect } from "react-redux";
import { component1 } from "../Components/component1";
import action from ''

const X = state =>({

})

const Y = dispatch => ({

})

export default connect(X , Y)(component1)